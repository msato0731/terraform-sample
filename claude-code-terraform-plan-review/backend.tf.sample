terraform {
  backend "s3" {
    bucket       = "<STATE_BUCKET>"
    key          = "claude-code-terraform-plan-review/terraform.tfstate"
    region       = "ap-northeast-1"
    use_lockfile = true
  }
}
